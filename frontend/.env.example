# Frontend environment.
# Leave VITE_API_URL empty to keep the app on its built-in demo data.
# Set it to the Express API to make every connected module database-driven.
VITE_API_URL=http://localhost:5000/api
